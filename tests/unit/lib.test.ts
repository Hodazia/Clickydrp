/*
write a simple test to check if everything is working or not!

- All the tests are defined in the folder tests/unit
- Vitest configurations are written in vitest.config.ts
- We are using vitest to run the test
- Now how to run the tests, `npx vitest run`
- One important thing don't use alias exports it will throw errors, to use it
we have to install another library, i am currently not installing it,
- unit/
    schema.test.ts
    db.test.ts 
    api.test.ts -> All the api testing done here!
*/


import  { assert, describe, it, expect } from "vitest"


// describe("add 1 and 2", () => {
//     it("it shall return 3", () => {
//         expect(1+2).eq(3);
//     })

//     it("foo",  () => {
//         assert.equal(Math.sqrt(4),2)
//     })
// })

